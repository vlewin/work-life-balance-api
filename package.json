{
  "scripts": {
    "start": "serverless offline --host 0.0.0.0 start -s development",
    "jest": "NODE_ENV=test jest --bail --watch",
    "test": "NODE_ENV=test jest",
    "test:unit": "NODE_ENV=test jest ./tests/unit  --silent",
    "test:functional": "NODE_ENV=test jest ./tests/functional",
    "test:watch": "NODE_ENV=test jest ./tests --watch",
    "package": "yarn run transpile; sls package -v",
    "deploy": "yarn install --production --ignore-scripts --prefer-offline; sls deploy -s development; yarn",
    "deploy:production": "yarn install --production --ignore-scripts --prefer-offline; sls deploy -s production; yarn"
  },
  "dependencies": {
    "ajv": "^6.4.0",
    "date-fns": "^1.29.0",
    "dynamoose": "^0.8.7",
    "jjv": "^1.0.2",
    "jjve": "^0.5.2",
    "jsonwebtoken": "^8.2.1",
    "jwk-to-pem": "^2.0.0",
    "request": "^2.85.0",
    "serverless-offline": "^3.20.0"
  },
  "devDependencies": {
    "axios": "^0.18.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-2": "^6.24.1",
    "eslint": "^4.17.0",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.11.0",
    "eslint-plugin-jest": "^21.15.1",
    "eslint-plugin-node": "^6.0.1",
    "eslint-plugin-promise": "^3.7.0",
    "eslint-plugin-standard": "^3.0.1",
    "jest": "^22.4.3"
  }
}
